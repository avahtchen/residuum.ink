baseURL = 'https://residuum.ink/'
languageCode = 'en-us'
title = 'Residuum'
theme = 'ink'

[outputs]
  home = ["HTML", "JSON", "RSS"]
  section = ["HTML", "JSON", "RSS"]
  page = ["HTML", "JSON"]

[params]
  description = "What remains. A site for humans and agents."
  author = "Ava Chen"
